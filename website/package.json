{
  "name": "magicpush-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "export VITE_BASE_PATH=/magicpush/ && vitepress build && cd .vitepress/dist && git add . && git commit -m 'deploy: update site static files' && git push origin gh-pages"
  },
  "dependencies": {
    "vitepress": "^1.3.4"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.25.0"
    }
  }
}
