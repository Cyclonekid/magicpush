---
title: 小爱音箱推送渠道配置指南
description: 通过小爱音箱实现语音播报推送的完整配置教程
outline: deep
---

# 小爱音箱推送渠道配置指南#

本教程将指导你如何在 MagicPush（魔法推送）中配置**小爱音箱**推送渠道，实现向小爱音箱发送语音播报通知。

## 概述#

### 什么是小爱音箱推送？

通过 **xiaoii** 底层 Speaker 模块，MagicPush 可以直接调用小米 IoT API，将文本消息发送至小爱音箱进行**语音播报**。

> ⚠️ **重要**：这是**语音播报**渠道，消息会以**语音**形式从小爱音箱播放出来，不是文字通知。

| 特点 | 说明 |
|------|------|
| 推送目标 | 小爱音箱（需在同一小米账号下） |
| 鉴权方式 | 小米 ID + PassToken（推荐）或密码 |
| 配置复杂度 | 中，需要小米账号信息 |
| 消息格式 | text（纯文本，自动转为语音） |
| 额外功能 | TTS 模式选择（auto/command/default） |

### 前置条件#

- 拥有**小米账号**和**小爱音箱**设备
- 已获取小米 ID（在账号信息中查看）
- 已部署并登录 MagicPush 管理后台
- （推荐）已获取 PassToken（避免密码登录被安全拦截）

---

## 第一步：获取小米账号配置信息#

### 1.1 获取小米 ID#

1. 访问 [小米账号中心](https://account.xiaomi.com/)
2. 登录后，点击 **「个人信息」**
3. 找到 **「用户 ID」**（纯数字）

> 📌 **小米 ID 示例**：`1234567890`

> 💡 **提示**：小米 ID 是纯数字，不是手机号或邮箱。

### 1.2（推荐）获取 PassToken#

使用 PassToken 登录更稳定，不会被小米安全验证拦截。

获取方式参考：[xiaoii 配置详解](https://github.com/xvhua n/xiaoi#%E9%85%8D%E7%BD%AE%E8%AF%A6%E8%A7%A3)

> 📌 **PassToken 获取步骤概要**：
> 1. 在浏览器中登录小米账号
> 2. 打开开发者工具（F12），查看网络请求
> 3. 找到 `serviceToken` 或类似字段
> 4. 复制完整的 Token 值

> 🔐 **重要**：PassToken 是敏感凭证，请妥善保管。

### 1.3（不推荐）使用密码登录#

如果不使用 PassToken，可以直接使用小米账号密码。

> ⚠️ **注意**：密码登录可能被小米安全验证拦截，导致登录失败。建议优先使用 PassToken。

### 1.4 获取音箱设备标识#

1. 打开 **米家 App**
2. 找到你的 **小爱音箱** 设备
3. 点击进入设备详情页
4. 记录设备名称（如 `客厅小爱`）或设备 DID（在开发者模式中查看）

> 📌 **设备标识**：
> - 可以直接使用**设备名称**（如 `客厅小爱`）
> - 或使用**设备 DID**（更精准，推荐）

至此，你已获得配置所需的信息：

| 配置项 | 示例值 | 来源 | |
|--------|--------|------|
| 小米 ID | `1234567890` | 小米账号中心 → 个人信息 | |
| PassToken（推荐） | `xxxxxxxxxxxxxxxx` | 参考 xiaoii 文档获取 | |
| 密码（不推荐） | `your_password` | 小米账号密码 | |
| 音箱设备标识 | `客厅小爱` 或 `did:xxxxx` | 米家 App → 设备详情 | |

---

## 第二步：在 MagicPush 中添加渠道#

### 2.1 进入渠道管理#

1. 登录 MagicPush 管理后台（默认地址 `http://<服务器IP>:3000`）
2. 点击左侧导航 **「渠道管理」**
3. 点击右上角 **「+ 绑定渠道」** 按钮#

### 2.2 选择渠道类型#

在弹出的对话框中，从渠道类型下拉列表中选择 **「小爱音箱」**。

### 2.3 填写配置信息#

根据第一步获取的信息，填写以下配置字段：

| 字段 | 说明 | 示例 | |
|------|------|------|
| **小米 ID** | 小米账号的数字 ID | `1234567890` | |
| **PassToken**（推荐） | 身份验证 Token，推荐填写此项 | `xxxxxxxxxxxxxxxx` | |
| **密码**（不推荐） | 小米账号密码，可能被安全拦截 | `your_password` | |
| **音箱设备标识** | 音箱名称或设备 DID | `客厅小爱` 或 `did:12345` | |
| **TTS 模式**（可选） | 语音播报模式 | `auto`（推荐）、`command`、`default` | |

> 💡 **PassToken vs 密码**：
> - **推荐使用 PassToken**：登录更稳定，不易被安全拦截
> - **不推荐密码**：可能被小米安全验证拦截，导致登录失败
> - PassToken 和**密码至少填写一个**

> 💡 **TTS 模式说明**：
> | 模式 | 说明 |
> |------|------|
> | `auto`（推荐） | 自动选择最优方式 |
> | `command` | 仅使用 MiOT 指令 |
> | `default` | 仅使用 MiNA 默认链路 |

填写完成后，给渠道起一个易于辨识的**名称**（如「客厅小爱音箱」），点击 **「保存」**。

### 2.4 测试连通性#

渠道创建成功后，在渠道卡片右侧的下拉菜单中，点击 **「测试」** 按钮。

- ✅ 如果小爱音箱播报「这是一条来自魔法推送的测试消息」，说明配置成功
- ❌ 如果测试失败，请参考下方[常见问题](#常见问题)排查#

> 💡 **提示**：语音播报可能有 1-3 秒延迟，请耐心等待。

---

## 第三步：使用推送#

### 3.1 通过 API 推送#

创建渠道后，可以通过 MagicPush 的标准 API 进行推送：

```bash
curl -X POST http://<服务器IP>:3000/api/push/<渠道ID> \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <你的API Token>" \
  -d '{
    "title": "系统告警",
    "content": "服务器 CPU 使用率超过 90%，请及时处理",
    "type": "text"
  }'
```

> 📌 小爱音箱**只支持 `text` 类型**，`markdown` 和 `html` 类型会自动转换为纯文本。

### 3.2 消息内容建议#

由于消息会以**语音**形式播报：

1. **简洁明了**：避免使用特殊符号、Markdown 标记
2. **避免过长**：语音播报过长体验不佳，建议 **500 字符以内**
3. **使用口语化表达**：如「服务器 CPU 使用率超过百分之九十」比「CPU > 90%」更易听懂#

MagicPush 会自动：
- 将 Markdown 转为纯文本
- 将 HTML 转为纯文本
- 截断超过 500 字符的内容（小爱 TTS 有长度限制）

---

## 技术细节#

### 消息长度限制#

- 小爱音箱 TTS：**500 字符**（超过自动截断）

### TTS 模式详解#

| 模式 | 说明 | 推荐场景 | |
|------|------|----------
| `auto`（推荐） | 智能选择最优方式 | 大多数场景 | |
| `command` | 仅使用 MiOT 指令 | auto 失败时使用 | |
| `default` | 仅使用 MiNA 默认链路 | 特殊场景 | |

### 认证过期自动重试#

- 如果认证过期（token 失效），MagicPush 会自动重新初始化
- 重新初始化后重试一次，无需手动干预#

---

## 常见问题#

### Q: 发送消息返回「认证失败」错误？

**原因**：PassToken 或密码填写错误。

**解决**：
1. 检查 PassToken 是否完整复制，没有多余空格或换行
2. 如果使用了密码，尝试改为使用 PassToken
3. 重新获取 PassToken 并填写#

### Q: 小爱音箱没有播报消息？

**原因**：可能有多种情况。

**解决**：
1. 确认小爱音箱在线（通电、联网正常）
2. 确认设备标识填写正确（音箱名称或 DID）
3. 检查 MagicPush 服务器能否访问小米 IoT API
4. 测试消息可能有 1-3 秒延迟，请耐心等待
5. 查看 MagicPush 日志，确认是否有错误信息#

### Q: 提示「PassToken 和密码至少需要填写一个」？

**原因**：两种登录方式都没有配置。

**解决**：
1. **推荐**：填写 PassToken（参考 [xiaoii 文档](https://github.com/xvhua n/xiaoi#%E9%85%8D%E7%BD%AE%E8%AF%A6%E8%A7%A3) 获取）
2. 或填写小米账号密码（可能被安全拦截）#

### Q: 音箱设备标识应该填什么？

**解决**：
1. **方式一**：填写在米家 App 中显示的设备名称（如 `客厅小爱`）
2. **方式二**（更精准）：获取设备 DID，填写 `did:xxxxx` 格式
3. 如果不确定，可以在米家 App 中查看设备详情#

### Q: 语音播报内容不清晰？

**原因**：消息内容包含特殊符号或过长。

**解决**：
1. 简化消息内容，使用口语化表达
2. 避免使用特殊符号（`%`、`#`、`@` 等）
3. 控制消息长度在 100 字符以内最佳#

### Q: TTS 模式应该选择哪个？#

| 场景 | 推荐模式 |
|------|----------
| 不确定 | `auto`（让系统自动选择） |
| auto 模式失败 | `command` |
| 需要使用默认链路 | `default` |

---

## 参考资源#

- [xiaoii GitHub](https://github.com/xvhua n/xiaoi)
- [xiaoii 配置详解](https://github.com/xvhua n/xiaoi#%E9%85%8D%E7%BD%AE%E8%AF%A6%E8%A7%A3)
- [小米 IoT 开放平台](https://iot.mi.com/)
- [MagicPush GitHub 仓库](https://github.com/magiccode1412/magicpush)
